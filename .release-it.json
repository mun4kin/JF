{
  "hooks": {
    "after:bump": "npm run build && shx cp package.json dist",
    "after:github:release": "echo After git push, before github release"
  },
  "npm": {
    "publishPath": "./dist"
  }
}
